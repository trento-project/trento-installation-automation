version: 2
updates:
  - package-ecosystem: "github-actions"
    directories:
      - "/"
      - "/.github/actions/*"
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 14
    groups:
      # A single pull request for every action bump, across all directories
      github-actions:
        patterns:
          - "*"
